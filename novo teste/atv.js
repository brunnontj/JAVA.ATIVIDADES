function login() {
    const emaill = document.getElementById("e-mail").value;
    const senha = document.getElementById("senha").value;


if (email === "aluno2" && senha === "senai") {
  alert("login bem-sucedido!");
} else {
    alert("crendeciais onválidas.tente novamente.");
}

}
